import React, { useState } from 'react';
import { useThemeAndImage, THEMES } from '../context/ThemeAndImageContext';
import { sisterData } from '../data/sisterData';
import { Palette, Image as ImageIcon, X, Upload, RefreshCw, Check, Sparkles, SlidersHorizontal, Moon, Sun } from 'lucide-react';

export default function ThemeAndImageControls() {
  // We intentionally hide the floating theme/photo controls to keep the UI clean.
  return null;
}
